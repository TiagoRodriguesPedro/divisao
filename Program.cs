using System;

namespace divisao
{
    class Program
    {
        static void Main(string[] args)
        {
            int numerador, denominador;

            // digite o numerador
            Console.Write("digite um numerador ---");
            numerador = Convert.ToInt32(Console.ReadLine());

            // digite o denominador
            Console.WriteLine("denominador---");
           numerador = Convert.ToInt32(Console.ReadLine());

            // 12 dividido por 3 é 12
            if (numerador > 12 / 3 );
            {
             Console.WriteLine(" demoninador");
            }
            
        }
    }
}
